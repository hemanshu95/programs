#include<stdio.h>
int main()
{
    int t,A[10000],l,i,q;
    char s[100];
    scanf("%d",&t);
    while(t-->0)
    {
        scanf("%d",&l);
        for(i=0;i<l;i++)
            scanf("%d",&A[i]);
        scanf("%d",&q);
        for(i=0;i<q;i++)
        {
            scanf("%d",s);
        }

    }
    return 0;
}
