#include<stdio.h>

int main()
{
    char a[100], b[100];

   scanf("%s",a);
     gets(b);q
     // scanf("%s",b);
    puts("h");
    puts(a);
    puts("h");
   // putchar(b[0]);
    // putchar(b[1]);
    printf("%s %s",a,b);

   // puts(b);
    puts("hello");
    //printf("bh");
    //puts (a);
    return 0;
}
