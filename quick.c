#include<stdio.h>
int sort(int A[],int x,int y)
{
    int j=x,,t1;

    for(i=x+1;i<=y;i++)
    {
        if(A[i]<temp)
        {
            t1=A[j];
            A[j]=A[i];
            A[i]=t1;
            j++;
        }
        if(A[i]==temp)
            pivot=i
    }
}
